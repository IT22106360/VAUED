
  # VAUED

  This is a code bundle for VAUED. The original project is available at https://www.figma.com/design/jmFi6064i72fLRPNLa4qVf/VAUED.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  